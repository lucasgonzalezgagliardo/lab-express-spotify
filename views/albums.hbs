<!-- artist-search-results.hbs -->
<h1>Albums</h1>
<a href="/tracks/{{album.id}}">Ver pistas</a>

<ul>
  {{#each albums}}
    <li>
      <img src="{{images.[0].url}}" alt="{{name}}">
      <h2>{{name}}</h2>
    </li>
  {{/each}}
</ul>
